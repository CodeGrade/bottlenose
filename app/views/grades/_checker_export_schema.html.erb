<div class="panel panel-info">
  <div class="panel-heading">
    <p>An archive containing:</p>
<pre>
assignment_<%= @assignment.id %>/
  &lt;submissionId&gt;.tap: the current TAP output for each submission
     OR
  &lt;submissionId&gt;.log: the current log file if there is no TAP output
</pre>
    <p>Each TAP file is generated by the tester library.  Each file is of the form:</p>
<pre>
TAP version 13
1..<i><b>Number of comments</b></i>
# Time: <i><b>Amount of time taken to run the grader, in seconds</b></i>
# TOTAL POINTS: <i><b>Total available points</b></i>
# Tests run: <i><b>Number of comments</b></i>, Failures: <i><b>Number of comments</b></i>
<i>zero or more tests</i>
</pre>
    <p>Successful tests are of the form:</p>
<pre>
ok <i><b>Test number</b></i>      <i><b>Name of tester method used</b></i>
# More information
  ---
  message: "<i><b>Description string of test</b></i>"
  weight: <i><b>How many points this test is worth</b></i>
  suppressed: <i><b>Should this penalty be ignored? (true or false)</b></i>
  <i>possibly other fields here, described below</i>
  ...
</pre>
    <p>Each failed test is of the form</p>
<pre>
not ok <i><b>Test number</b></i>      <i><b>Name of tester method used</b></i>
# More information
  ---
  message: "<i><b>Description string of test</b></i>"
  weight: <i><b>How many points this test is worth</b></i>
  suppressed: <i><b>Should this penalty be ignored? (true or false)</b></i>
  <i>possibly other fields here, described below</i>
  ...
</pre>
    <p>Lines beginning with <code>#</code> are comments. The <code>---</code> and
      <code>...</code> are literal text, not placeholders.</p>
    <p>The tester library produces several test results, which each
      have distinct information fields (in addition to the three above):</p>
    <div class="form-tabs" data-init-tab="TestEquality">
      <ul class="nav nav-tabs">
        <li><a data-tab="TestEquality">Equality</a></li>
        <li><a data-tab="TestInexactEquality">Inexact Equality</a></li>
        <li><a data-tab="TestMethodInvocation">Method Invocation</a></li>
        <li><a data-tab="TestCorrectException">Correct Exception</a></li>
        <li><a data-tab="TestNoException">No Exception</a></li>
        <li><a data-tab="TestFailure">Failure</a></li>
        <li><a data-tab="StackOverflow">Stack Overflow</a></li>
        <li><a data-tab="TestTimedOut">Time Out</a></li>
      </ul>
      <div class="form-tabs-content tab-content" style="color: black; background: white;">
        <div class="tab-pane" data-tab="TestEquality">
          <p>
            Equality test results just confirm whether two values are the same.
          </p>
<pre>
  ---
  actual: [
    "<i><b>Array of strings</b></i>",
    "<i><b>showing each line of the actual output of the test</b></i>"
  ],
  expected: [
    "<i><b>Array of strings</b></i>",
    "<i><b>showing each line of the expected output of the test</b></i>"
  ],
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="TestInexactEquality">
          <p>
            Inexact-equality test results allow for tolerances when
            checking <code>double</code> values.
          </p>
<pre>
  ---
  tolerance: <i><b>The tolerance used</b></i>,
  actual: [
    "<i><b>Array of strings</b></i>",
    "<i><b>showing each line of the actual output of the test</b></i>"
  ],
  expected: [
    "<i><b>Array of strings</b></i>",
    "<i><b>showing each line of the expected output of the test</b></i>"
  ],
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="TestMethodInvocation">
          <p>
            Method-invocation test results show when a method or constructor
            call fails.
          </p>
<pre>
  ---
  methodName: "<i><b>Name of the method (or constructor) being called</b></i>",
  paramNames: [
    "<i><b>Array of individual</b></i>",
    "<i><b>parameter names to the method</b></i>"
  ],
  args: [
    [
      "<i><b>Array of individual argument values</b></i>",
      "<i><b>rendered as arrays of strings (one per line)</b></i>"
    ]
  ],
  expected: [
    "<i><b>Array of strings</b></i>",
    "<i><b>showing each line of the expected output of the test</b></i>"
  ],
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="TestCorrectException">
          <p>
            Correct-exception test results show when a method or constructor
            call throws the intended exception.
          </p>
<pre>
  ---
  targetClassName: "<i><b>Name of receiver class and method</b></i>",
  actualExnName: "<i><b>Name of the actual exception that was thrown</b></i>",
  actualExnMessage: "<i><b>Message in the actual thrown exception</b></i>",
  expectedExn: "<i><b>Name of the expected exception to be thrown</b></i>",
  expectedExnMessage: "<i><b>Message expected to be thrown</b></i>",
  arguments: [
    [
      "<i><b>Array of individual argument values</b></i>",
      "<i><b>rendered as arrays of strings (one per line)</b></i>"
    ]
  ],
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="TestNoException">
          <p>
            No-exception test results show when a method or constructor
            call properly avoids throwing any exception.
          </p>
<pre>
  ---
  targetClassName: "<i><b>Name of receiver class and method</b></i>",
  actualExnName: "<i><b>Name of the actual exception that was thrown</b></i>",
  actualExnMessage: "<i><b>Message in the actual thrown exception</b></i>",
  arguments: [
    [
      "<i><b>Array of individual argument values</b></i>",
      "<i><b>rendered as arrays of strings (one per line)</b></i>"
    ]
  ],
  stack: [
    "<i><b>Array of individual stack frames</b></i>",
    "<i><b>one per line</b></i>"
  ]
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="TestFailure">
          <p>
            Failure test results simply show an unexpected test failure
          </p>
<pre>
  ---
  stack: [
    "<i><b>Array of individual stack frames</b></i>",
    "<i><b>one per line</b></i>"
  ]
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="StackOverflow">
          <p>
            Stack-overflow test results occur when a test fails due to
            a stack overflow.  They record their stack as compressed
            as possible, so as to avoid wasting huge amounts of disk space.
          </p>
<pre>
  ---
  stack: [
    "<i><b>Array of individual stack frames</b></i>",
    "<i><b>one per line</b></i>"
  ]
  ...
</pre>
        </div>
        <div class="tab-pane" data-tab="TestTimedOut">
          <p>
            Timed-out test results occur when a test fails to complete
            in an allotted time.
          </p>
<pre>
  ---
  timeout: <i><b>Timeout for this test case</b></i>,
  stack: [
    "<i><b>Array of individual stack frames</b></i>",
    "<i><b>one per line</b></i>"
  ]
  ...
</pre>
        </div>
      </div>
    </div>
  </div>
</div>
