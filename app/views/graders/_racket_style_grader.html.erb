<%= f.hidden_field :type, value: "RacketStyleGrader" %>
<%= f.hidden_field :order %>

<table class="table no-first-row" style="margin-bottom: 0px;">
  <tr>
    <td><%= f.label "avail_score", "Points available:" %></td>
    <td><%= f.spinner "avail_score", f.object.avail_score || 50, :min => 0, :delta => 5 %></td>
  </tr>
  <tr>
    <td><%= f.label "line_length", "Maximum line length:" %></td>
    <td><%= f.spinner "line_length", f.object.line_length || f.object.default_line_length, :min => 0 %></td>
  </tr>
</table>
